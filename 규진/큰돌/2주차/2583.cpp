#include <iostream>

using namespace std;

int dy[4] = {-1, 0, 1, 0};
int dx[4] = {0, 1, 0, -1};
int M, N, K;
int visited[];


int main(void) {
    cin >> M >> N >> K;
    for (int i = 0; i < K; i++) {
        int a, b, c, d;
        cin >> a >> b >> c >> d;
        // for (int j = a;)
    }
    return 0;
}